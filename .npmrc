; better-auth ships a peer range that npm's strict resolver rejects; the project
; is verified to work with the resolved tree. This keeps `npm ci` / `npm install`
; working without callers needing to pass --legacy-peer-deps every time.
legacy-peer-deps=true
