---
eyebrow: Beyond conversion
title: The workflow around the engine
lede: The conversion is commoditized. The value is everything wrapped around it, packaged for cloud users and self-hosters alike.
features:
  - name: Library with preview and raw
    body: Every converted document in a file viewer. Toggle rendered Markdown (GFM) against the syntax-highlighted source, then copy, download, or delete.
    icon: library
  - name: Originals kept, side by side
    body: Every uploaded file is stored on your own volume and previewed in place, with an Original vs Markdown toggle. The Markdown lives in Postgres.
    icon: files
  - name: Per-key transparency
    body: Issue revocable API keys. Every conversion an agent makes is cleaned, token-counted, saved to your Library, and attributed to its key, so you see exactly what it read.
    icon: shield
  - name: Token economics, counted
    body: Each conversion shows raw versus clean token counts and the cleaning breakdown, so the savings are a number you can see, not a claim.
    icon: gauge
  - name: RAG-ready output
    body: Clean Markdown with real headings and semantic boundaries, structured for chunking and embedding in your vector store.
    icon: boxes
  - name: Real-data dashboard
    body: Documents, tokens saved, agent conversions, originals stored, a 30-day activity chart, source breakdown, and top token-savers.
    icon: activity
---
