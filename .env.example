# Copy to .env.local and fill in as needed.
# Environment variables are validated by env.mjs (T3 Env) — add new vars there.

# Enable the Next.js bundle analyzer during `npm run build` ("true" | "false").
ANALYZE=false

# Skip env validation (used in CI builds). Leave unset locally.
# SKIP_ENV_VALIDATION=true
